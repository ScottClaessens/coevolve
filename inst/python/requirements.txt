# Pinned Python dependencies for the coevolve JAX backend.
# Install with: pip install -r requirements.txt
# Or via the R helper: coevolve:::setup_jax_env()
jax==0.5.3
jaxlib==0.5.3
numpyro==0.19.0
nutpie==0.16.8
